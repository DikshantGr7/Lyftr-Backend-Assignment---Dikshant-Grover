services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////data/messages.db
    volumes:
      - ./data:/data
    restart: always

volumes:
  data: